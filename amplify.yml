version: 1
backend:
  phases:
    build:
      commands:
        - nvm install 20.11.1
        - nvm use 20.11.1
        - npm install
        - npm ci
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
